# Pala Note partition table (4 MB flash).
# Adds a small `pala_cfg` data partition that the Pala Cloud browser flasher
# writes provisioning into (Wi-Fi, OpenAI key, device key, host). Keep the
# pala_cfg offset (0x310000) in sync with public/firmware/manifest.json and
# lib/pala-device-config.ts in the pala-cloud repo.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xe000,   0x2000,
app0,       app,  ota_0,    0x10000,  0x300000,
pala_cfg,   data, 0x40,     0x310000, 0x1000,
spiffs,     data, spiffs,   0x311000, 0xEF000,
